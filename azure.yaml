# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-flask-cosmos-mongodb-aca
metadata:
    template: azure-flask-cosmos-mongodb-aca
infra:
    provider: "bicep"
services:
    web:
        project: src
        language: py
        host: appservice
